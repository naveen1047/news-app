import 'package:flutter/painting.dart';
import 'constant.dart';

const TextStyle kTitle = TextStyle(fontSize: kTitleSize);
const TextStyle kDescription = TextStyle(fontSize: kDescrptionSize);
const TextStyle kDefaultText = TextStyle(fontSize: kDefaultTextSize);

const EdgeInsetsGeometry ktopPadding = const EdgeInsets.only(top: 8.0);
